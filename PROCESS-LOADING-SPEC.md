# AI 对话过程加载说明

> 当前规则版本：2026-09-14。本文描述 `ai-conversation.html` 的过程展示、编组、滚动与加载交互，也是后续接入真实智能体事件流的约定。

## 1. 设计目标

过程区不是智能体的原始日志。它只需要让用户持续确认三件事：任务仍在运行、现在大致在做什么、是否出现了需要关注的问题。参数、命令输出、推理细节和历史动作始终保留，但默认收起，用户需要时再展开。

这套规则适用于资料检索、文件编辑、代码执行、外部工具调用、工作空间初始化、子任务编排和未知的后续智能体能力。页面现在使用模拟事件演示；接入服务端后，仍由同一套规则渲染。

## 2. 一轮回答的状态

| 轮次状态 | 过程区与回答区行为 |
| --- | --- |
| `queued` | 已收到任务，通常并入邻近的操作组，不单独占一行。 |
| `processing` | 过程默认展开，显示用户可理解的进度、当前操作和异常。页面可跟随最新过程。 |
| `streaming` | 过程先自动收起，再从正文开头流式输出回答；正文阶段不自动追底。 |
| `complete` | 正文输出完成；过程保持收起，可随时重新查看。 |
| `stopped` | 已停止原因和已保留内容外显。 |
| `failed` | 失败原因与可恢复信息外显，历史过程保留。 |
| `timeout` | 超时与已完成内容外显。 |
| `confirm` | 需要用户确认时外显，不能编入普通操作组。 |

过程完成与正文开始不是同一个时点：过程收起动画结束后即可开始流式输出，用户不必等待正文全部完成才看到回答。

## 3. 用户可见性规则

过程事件按原始顺序完整保存。展示层只决定默认露出什么，绝不删除事件。

生成过程中，主时间线优先露出以下内容：

1. **助手文字进度**：阶段说明、已确认结论、下一步计划。
2. **当前操作**：正在运行的一条最新命令，或正在执行的一组操作中的最新一条。
3. **异常与阻塞**：失败、超时、已停止、需要确认，以及会影响结果的部分完成。

以下内容默认作为过程细节收起：排队、等待、能力加载、工作空间准备、读取、检索、编辑、命令参数、命令输出、内部思考、上下文压缩、产物写入和已完成的普通操作。用户展开对应组后，仍可按发生顺序查看全部内容。

因此，用户看到的节奏应接近：

`阶段说明 → 当前操作组 → 阶段说明 → 异常或确认 → 下一操作组`

而不是连续铺开的内部日志。

## 4. 状态编组规则

### 4.1 分组边界

连续的非文本、非异常过程事件进入同一个操作组。以下事件会结束当前组，并形成新的阅读断点：

- 助手向用户展示的文字进度；
- 失败、超时、停止、需要确认等异常或阻塞状态；
- 轮次结束。

队列、等待、内部思考、上下文压缩、读取、检索、编辑、运行命令、能力加载、子任务、并行任务和产物生成都可以进入操作组。这样连续执行时不会出现多条命令逐行堆叠。

### 4.2 何时创建组

- **两条或以上连续事件**：创建可展开的操作组。
- **只有一条事件**：直接展示该事件，不再额外套一层“组”，避免标题和内容重复。
- **单个深度思考**：直接展示为深度思考；若它处于连续内部过程里，则随该过程组收起。
- **单个命令**：直接展示，可展开查看参数和结果。

### 4.3 运行中与完成后的标题

操作组还在运行时，只显示最新一条具体动作，例如“正在运行数据映射校验”。这样用户始终能知道当前在做什么。

当该组结束并进入下一段文字、异常、确认或下一轮操作后，标题从最后一条命令切换为完成概括，避免历史一直停留在某个过时的命令名称上。概括的生成顺序如下：

1. 使用事件提供的 `actionSummary`；
2. 按 `actionKind` 归类；
3. 某一类别占组内大多数时，使用该类别；
4. 仅含读取与校验时，使用“资料读取与校验”；
5. 无法可靠分类时，使用“已完成 N 项操作”。

示例：

| 连续操作 | 运行中 | 完成后 |
| --- | --- | --- |
| 读取规范、扫描依赖、检索历史资料、建立快照 | 正在建立执行快照 | 已完成运行规范与历史资料检查（4 项） |
| 读取资料、核对字段、运行校验 | 正在运行数据映射校验 | 已完成资料分析与数据校验（3 项） |
| 多种未知工具混合调用 | 正在执行最新动作 | 已完成 5 项操作 |

`actionSummary` 应使用简短的用户语言，如“资料读取与校验”，不应包含“已完成”或数量；页面统一补充状态和数量。

### 4.4 分组展开状态

- 每个事件、操作组和子任务卡片都有稳定的唯一键。
- 点击某个组、子项详情或深度思考，只改变该节点自身的展开状态；其他节点不跟随展开或收起。
- 新事件到达、过程重绘或组标题从运行中切换为完成概括时，已经打开的其他节点保持原状。
- 完成的操作组默认收起。完成的子任务卡也默认收起；运行中的子任务卡可默认展开以显示进度。

## 5. 特殊节点

### 深度思考

- 用于展示对用户有价值的阶段性思路，不用于逐 token 暴露内部推理。
- 运行中可用轻微、克制的动态状态提示仍在处理。
- 内容过长时先显示限定高度，并提供“展开全部”。
- 用户展开过程或深度思考时，从顶部开始阅读。
- 过程更新不会让已显示的深度思考卡重复播放入场动画，避免闪烁。

### 子任务与并行任务

- 子任务属于操作事件，连续出现时和其他操作一起收进操作组。
- 只有先展开外层操作组，才显示子任务卡片；再展开卡片，才能看到任务清单和每项状态。
- 标题、摘要和箭头固定在同一行，箭头常驻最右侧；展开内容不应推动标题位置跳动。
- 子任务超时、失败或停止的影响仍要在主时间线外显，不能因任务卡片折叠而被隐藏。

### 异常、停止、超时与确认

失败、超时、停止、需要确认及影响交付的部分完成，都是用户需要立即感知的状态：

- 不参与自动编组；
- 使用独立卡片说明发生了什么、已保留什么、接下来能做什么；
- 即使前后都是操作，也要切断操作组；
- 有背景的异常或任务卡片，图标与卡片边缘保留至少 8px 安全间距。

### 产物与最终回答

- 报告、文件、草案和最终交付说明可作为过程事件保留。
- 回答正文开始前，过程区域自动收起并播放过渡动画。
- 回答完成后显示图标操作：复制内容、复制 Markdown、生成时间。
- 数字人、输入区和滚动内容允许视觉重叠，不为形象留出无效空白。

## 6. 滚动与加载提示

### 6.1 过程阶段的自动跟随

过程处于 `processing` 时，页面默认跟随最新过程，最新正在执行的操作保持可见。过程列表达到最大高度后，列表在内部滚动。

以下任一操作代表用户开始查看历史内容，必须立即暂停自动跟随：

- 用户向上滚动页面；
- 用户点击或通过键盘展开/收起任意过程节点；
- 用户展开子项详情、操作组、子任务或深度思考；
- 用户在过程列表内手动查看细节。

暂停后，后续事件、详情展开和过程重绘都不能把页面或过程列表强制拉到底部。只有下列行为可以恢复跟随：

- 用户主动滚回页面最底部；
- 用户点击“跳到最新内容”提示。

鼠标停在过程列表上时，只有列表本身还能继续向该方向滚动才消耗滚轮；列表已经到顶或到底时，滚动继续传递给外层页面，以便查看正文。

### 6.2 未读内容提示

当用户没有停在底部、下方仍有新内容时，输入框上方居中显示圆形提示：

| 轮次状态 | 提示图标 | 点击结果 |
| --- | --- | --- |
| `processing` 或 `streaming` | 圆圈内三个点动画 | 跳到最新内容，并恢复过程阶段跟随。 |
| `complete`、`stopped`、`failed` 或 `timeout` | 向下箭头 | 一次跳到当前轮次末尾。 |

提示只在确实存在未读下方内容时显示。过程自动收起并开始正文流式输出后，页面不再主动追底；用户从正文开头向下阅读，需要时自行滚动或点击提示。

## 7. 动画与视觉反馈

- 手动展开、手动收起和过程完成时的自动收起使用同一类高度与透明度过渡，过程清晰且不突兀。
- 流式更新只更新内容与必要的状态，不重新播放既有卡片的入场动画，避免卡片闪烁。
- 运行中的提示动画只用于表达“仍在进行”，不用于给每张过程卡片增加持续动画。
- 普通过程图标保持统一的尺寸、颜色和纵向基线；任务、异常等带背景的卡片可内缩，不要求和普通命令图标完全对齐。
- 所有动态效果应遵守系统“减少动态效果”偏好。

## 8. 推荐事件协议

接入真实服务时，每个事件必须有稳定的 `id` 和明确的语义字段；前端不应仅靠展示文案猜测操作类型。

```ts
type ProcessEvent = {
  id: string;
  timestamp: number;
  status: 'queued' | 'running' | 'complete' | 'failed' | 'stopped' | 'timeout' | 'confirm';
  kind:
    | 'assistant'
    | 'thinking'
    | 'command'
    | 'task'
    | 'parallel'
    | 'waiting'
    | 'compaction'
    | 'artifact'
    | 'exception';
  actionKind?: 'setup' | 'read' | 'edit' | 'run' | 'verify' | 'task' | 'external' | 'unknown';
  actionSummary?: string;
  label: string;
  detail?: string;
  result?: string;
  taskItems?: Array<{
    id: string;
    label: string;
    status: 'queued' | 'running' | 'complete' | 'failed' | 'stopped' | 'timeout';
  }>;
  error?: {
    code?: string;
    message: string;
    retryable?: boolean;
  };
};
```

事件接收规则：

1. 以 `id` 追加或更新同一事件，不能因刷新文字创建新的视觉节点；
2. 保留服务端的发生顺序，并在客户端按上述边界即时重新编组；
3. 事件更新不能清空用户已打开的组、详情、子任务或“展开全部”状态；
4. 未识别的 `kind` 或 `actionKind` 归入普通操作，完成后使用中性标题“已完成 N 项操作”；
5. 失败、超时、停止和确认必须以明确终态推送，不能由超时的沉默来推断。

## 9. 当前演示与验收

`ai-conversation.html` 以预设步骤和定时器模拟两类问题：普通资料审查，以及包含工作空间初始化、能力加载、读取与检索、代码与命令、长深度思考、并行子任务、等待、上下文压缩、失败、停止、超时、重试、产物和最终回答的复杂过程。它不执行真实命令，也不连接 AI 后端。

发布或接入真实服务前，至少验证：

- 单条操作不套组，连续操作按文本、异常和确认正确切组；
- 运行中显示最新动作，完成后按 `actionSummary`、`actionKind` 或通用兜底正确概括；
- 未知工具、混合工具、任务与并行任务不会破坏编组；
- 深度思考、长详情、长过程列表和内外层滚动边界正常；
- 展开任意子项后，其他组不联动，且页面不会自动追到底部；
- 滚回底部或点击圆形提示后，过程阶段重新跟随最新内容；
- 过程收起后正文从顶部开始流式输出，正文阶段不自动追底；
- 超时、失败、停止、部分完成与确认始终外显；
- 过程持续更新时卡片不闪烁，且尊重“减少动态效果”。
